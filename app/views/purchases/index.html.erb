<p id="notice"><%= notice %></p>

<h1 class="upper-title text-center" style="background-color: #FFA959;color: #fff; margin-top: 30px; padding: 40px "><span class="glyphicon glyphicon-briefcase"></span> Inventario</h1>

<div class="container">
  <table class="table">
    <thead>
      <tr>
        <th class="text-center">Cursos poseídos</th>
      </tr>
    </thead>

    <tbody>
      <% @purchases.each do |purchase| %>
        <tr class="text-center">
          <% if purchase.user_id == current_user.id %>
            <td><%= purchase.course.name %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<br>

