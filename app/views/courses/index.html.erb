<p id="notice"><%= notice %></p>

<h1 class="upper-title text-center">Courses</h1>

<div class="container text-center">
  <div class="row">
    <% @courses.each do |course| %>
    <div id="course" class="col-md-5">
      

      <h2><%= course.name %></h2>
      <p style="margin-bottom: 30px;">  <%= course.category %>  </p>
      <img style="max-height: 200px; max-height: 250px; margin: auto;" class="img-responsive" src="/assets/rubbish.jpg" alt="Img-test">
      
      <% if course.description.length < 135  %>

        <p style="margin-top: 30px; font-size: 1.2em"><%= course.description %></p>
      
      <% else %>
        <p style="margin-top: 30px; font-size: 1.2em"><%= course.description[0..135] %>...[<span class="text-warning">Ver Más Información</span>]></p>
        
      <% end %>

      <span class="text-primary" style="display: block; font-size: 1.2em">
        Price: <%= course.price %> 
      </span>
      <br>
      <%= link_to 'Más Información', course, class: "btn btn-primary btn-lg"%>

      
    </div>
    <% end %>
  </div>  
</div>

<br>

<%= link_to 'New Course', new_course_path %>
